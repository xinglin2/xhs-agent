[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/publisher/Dockerfile"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port 8001"
healthcheckPath = "/health"
healthcheckTimeout = 90
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[[services]]
name = "publisher"
